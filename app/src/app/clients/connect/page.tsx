import { ClientSyncOnboarding } from "~/features/clients/components/client-sync-onboarding";

export default function ClientConnectPage() {
  return (
    <main className="min-h-screen bg-slate-50">
      <ClientSyncOnboarding />
    </main>
  );
}

